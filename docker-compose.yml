version: "3"

services:
  frontend:
    container_name: detta_frontend
    restart: always
    build:
      context: ./frontend
      dockerfile: Dockerfile
    expose:
      - 3000
    environment:
      - TZ=Europe/Berlin
    networks:
      - dettaweb-network

  

  proxy:
    image: nginx:latest
    restart: always
    container_name: dettaweb_proxy
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    expose:
      - 80
    depends_on:
      - frontend
    networks:
      - dettaweb-network
      - traefik-main
    environment:
      - TZ=Europe/Berlin


networks:
  traefik-main:
  dettaweb-network:
    driver: bridge